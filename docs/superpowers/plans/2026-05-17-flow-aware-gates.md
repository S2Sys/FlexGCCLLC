# Flow-Aware Gates Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix five commands whose phase gates and dashboards are hardcoded to Architecture First — Full Stack. After PATH CONFIRMED is locked, these commands must read it to know which phases apply, what order they run in, and which error messages to show.

**Architecture:** PATH CONFIRMED in `docs/SRS.md` is the single source of truth. BREAKDOWN.md phase rows are written dynamically based on the locked flow. All gate checks read PATH CONFIRMED before validating phases.

**Tech Stack:** Markdown skill files in `.claude/commands/` — no code.

**Spec:** `docs/superpowers/specs/2026-05-17-path-confirmation-design.md`
**Depends on:** `docs/superpowers/plans/2026-05-17-path-confirmed-implementation.md` (Task 8 adds STEP 13 to swp-srs.md — this plan amends Task 8 to also generate BREAKDOWN.md phases)

---

### Task 1: Fix swp-srs.md STEP 12 step 7 — de-hardcode BREAKDOWN.md phases

**Files:**
- Modify: `.claude/commands/swp-srs.md` (lines ~1166–1187) — strip hardcoded §1A/§1B/§2A/§2B from step 7, keep only header + Phase 0

**Problem:** BREAKDOWN.md is generated with four hardcoded phase rows before the developer has chosen a flow. Frontend Only and API Only projects will never complete §1A, §1B, or §2A respectively — those phases don't exist in those flows.

**Fix:** Step 7 writes only the header + Phase 0 row. Flow-specific phases are appended by STEP 13 after PATH CONFIRMED is locked.

- [ ] **Step 1: Read swp-srs.md lines 1166–1188 to confirm exact old text**

- [ ] **Step 2: Replace the BREAKDOWN.md write block**

  Find and replace this exact block:
  ```
  7. Create docs/BREAKDOWN.md — full project phase skeleton:
     If docs/BREAKDOWN.md does not exist: create it fresh.
     If it already exists: preserve existing content and add any missing phase sections below.

     Write (or merge) the following structure into docs/BREAKDOWN.md:

     # Project Breakdown
     SRS Version : SRS-v[X.X]
     Generated   : [today's date]
     Generated by: /swp-srs

     ## Phase 0 — SRS Review            [x] [today's date]
       - SRS-v[X.X] approved. [N] Epics, [N] Must Have features.

     ## Phase 1A — Architecture Design   [ ] pending
     ## Phase 1B — Scaffold              [ ] pending
     ## Phase 2A — UI/UX Design          [ ] pending
     ## Phase 2B — DB Design             [ ] pending
     ## Phase 3  — Dev Stories           [ ] BLOCKED (requires 1A+1B+2A+2B complete)

       git add docs/BREAKDOWN.md
       (include in the docs commit below)
  ```

  Replace with:
  ```
  7. Create docs/BREAKDOWN.md — Phase 0 header (flow phases written in STEP 13):
     If docs/BREAKDOWN.md does not exist: create it fresh.
     If it already exists and already has flow phase rows below Phase 0: skip this step entirely.

     Write (or confirm present) the following in docs/BREAKDOWN.md:

     # Project Breakdown
     SRS Version : SRS-v[X.X]
     Generated   : [today's date]
     Generated by: /swp-srs
     Flow        : [PATH CONFIRMED not yet set — written by STEP 13]

     ## Phase 0 — SRS Review            [x] [today's date]
       - SRS-v[X.X] approved. [N] Epics, [N] Must Have features.

     ← Flow-specific phase rows appended by STEP 13 after PATH CONFIRMED is locked

       git add docs/BREAKDOWN.md
       (include in the docs commit below)
  ```

- [ ] **Step 3: Commit**

  ```
  git add .claude/commands/swp-srs.md
  git commit -m "fix(swp-srs): de-hardcode BREAKDOWN.md phases — flow rows written by STEP 13"
  ```

---

### Task 2: Amend original plan Task 8 — add BREAKDOWN.md generation to STEP 13

**Files:**
- Modify: `docs/superpowers/plans/2026-05-17-path-confirmed-implementation.md`

Task 8 in the original plan adds STEP 13 (PATH SELECTION) to swp-srs.md. The STEP 13 block must also append flow-specific phases to BREAKDOWN.md immediately after writing PATH CONFIRMED.

Add the following section to Task 8 in the original plan, after the "write PATH CONFIRMED block to SRS.md" step and before "Then commit":

```markdown
#### After writing PATH CONFIRMED — append flow phases to BREAKDOWN.md

Based on the locked flow, append the correct phase rows to docs/BREAKDOWN.md immediately
after the Phase 0 row. Use this lookup table:

**Flow 1A — Architecture First Full Stack:**
  ## Phase 1A — Architecture Design   [ ] pending  (command: /swp-arch)
  ## Phase 1B — Scaffold              [ ] pending  (command: /swp-arch Stage 2)
  ## Phase 2  — UI/UX + DB Design     [ ] pending  (command: /swp-design)
  ## Phase 3  — Dev Stories           [ ] BLOCKED  (requires 1A + 1B + 2 complete)

**Flow 1B — Architecture First Separate Design:**
  ## Phase 1A — Architecture Design   [ ] pending  (command: /swp-arch)
  ## Phase 1B — Scaffold              [ ] pending  (command: /swp-arch Stage 2)
  ## Phase 2A — UI/UX Design          [ ] pending  (command: /swp-ui)
  ## Phase 2B — DB Design             [ ] pending  (command: /swp-db)
  ## Phase 3  — Dev Stories           [ ] BLOCKED  (requires 1A + 1B + 2A + 2B complete)

**Flow 1C — Frontend First:**
  ## Phase 2A — UI/UX Design          [ ] pending  (command: /swp-ui)     ← step 1
  ## Phase 2B — DB Design             [ ] pending  (command: /swp-db)     ← step 2
  ## Phase 1A — Architecture Design   [ ] pending  (command: /swp-arch)   ← step 3
  ## Phase 1B — Scaffold              [ ] pending  (command: /swp-arch Stage 2) ← step 4
  ## Phase 3  — Dev Stories           [ ] BLOCKED  (requires 2A + 2B + 1A + 1B complete)

**Flow 1D — DB First:**
  ## Phase 2B — DB Design             [ ] pending  (command: /swp-db)     ← step 1
  ## Phase 1A — Architecture Design   [ ] pending  (command: /swp-arch)   ← step 2
  ## Phase 1B — Scaffold              [ ] pending  (command: /swp-arch Stage 2) ← step 3
  ## Phase 2A — UI/UX Design          [ ] pending  (command: /swp-ui)     ← step 4
  ## Phase 3  — Dev Stories           [ ] BLOCKED  (requires 2B + 1A + 1B + 2A complete)

**Flow 1E — Design System First:**
  ## Phase 2  — UI/UX + DB Design     [ ] pending  (command: /swp-design) ← step 1
  ## Phase 1A — Architecture Design   [ ] pending  (command: /swp-arch)   ← step 2
  ## Phase 1B — Scaffold              [ ] pending  (command: /swp-arch Stage 2) ← step 3
  ## Phase 3  — Dev Stories           [ ] BLOCKED  (requires 2 + 1A + 1B complete)

**Flow 2A — Arch First API:**
  ## Phase 1A — Architecture Design   [ ] pending  (command: /swp-arch)
  ## Phase 1B — Scaffold              [ ] pending  (command: /swp-arch Stage 2)
  ## Phase 2B — DB Design             [ ] pending  (command: /swp-db)
  ## Phase 3  — Dev Stories           [ ] BLOCKED  (requires 1A + 1B + 2B complete)

**Flow 2B — DB First API:**
  ## Phase 2B — DB Design             [ ] pending  (command: /swp-db)     ← step 1
  ## Phase 1A — Architecture Design   [ ] pending  (command: /swp-arch)   ← step 2
  ## Phase 1B — Scaffold              [ ] pending  (command: /swp-arch Stage 2) ← step 3
  ## Phase 3  — Dev Stories           [ ] BLOCKED  (requires 2B + 1A + 1B complete)

**Flow 3A — UI First Frontend Only:**
  ## Phase 2A — UI/UX Design          [ ] pending  (command: /swp-ui)
  ## Phase 3  — Dev Stories           [ ] BLOCKED  (requires 2A complete)

**Flow 4A — Arch First Mobile:**
  ## Phase 1A — Architecture Design   [ ] pending  (command: /swp-arch)
  ## Phase 1B — Scaffold              [ ] pending  (command: /swp-arch Stage 2)
  ## Phase 2  — UI/UX + DB Design     [ ] pending  (command: /swp-design)
  ## Phase 3  — Dev Stories           [ ] BLOCKED  (requires 1A + 1B + 2 complete)

**Flow 4B — Mobile First:**
  ## Phase 2A — UI/UX Design          [ ] pending  (command: /swp-ui)     ← step 1
  ## Phase 2B — DB Design             [ ] pending  (command: /swp-db)     ← step 2
  ## Phase 1A — Architecture Design   [ ] pending  (command: /swp-arch)   ← step 3
  ## Phase 1B — Scaffold              [ ] pending  (command: /swp-arch Stage 2) ← step 4
  ## Phase 3  — Dev Stories           [ ] BLOCKED  (requires 2A + 2B + 1A + 1B complete)

**Flow 5A — Mobile UI First BaaS:**
  ## Phase 2A — UI/UX Design          [ ] pending  (command: /swp-ui)
  ## Phase 3  — Dev Stories           [ ] BLOCKED  (requires 2A complete)

**Flow 6A — Arch First Serverless:**
  ## Phase 1A — Architecture Design   [ ] pending  (command: /swp-arch)
  ## Phase 1B — Scaffold              [ ] pending  (command: /swp-arch Stage 2)
  ## Phase 2B — DB/Event Schema       [ ] pending  (command: /swp-db)
  ## Phase 3  — Dev Stories           [ ] BLOCKED  (requires 1A + 1B + 2B complete)

**Flow 6B — Serverless + Frontend:**
  ## Phase 1A — Architecture Design   [ ] pending  (command: /swp-arch)
  ## Phase 1B — Scaffold              [ ] pending  (command: /swp-arch Stage 2)
  ## Phase 2  — UI/UX + DB Design     [ ] pending  (command: /swp-design)
  ## Phase 3  — Dev Stories           [ ] BLOCKED  (requires 1A + 1B + 2 complete)

**Flow 7A — Arch First Multi-service:**
  ## Phase 1A — Architecture Design   [ ] pending  (command: /swp-arch)
  ## Phase 1B — Scaffold              [ ] pending  (command: /swp-arch Stage 2)
  ## Phase 2B — DB Design (per svc)   [ ] pending  (command: /swp-db per service)
  ## Phase 3  — Dev Stories           [ ] BLOCKED  (requires 1A + 1B + 2B complete)

**Flow 7B — Contract First Multi-service:**
  ## Phase 2B — DB/Contract Design    [ ] pending  (command: /swp-db)     ← step 1
  ## Phase 1A — Architecture Design   [ ] pending  (command: /swp-arch)   ← step 2
  ## Phase 1B — Scaffold              [ ] pending  (command: /swp-arch Stage 2) ← step 3
  ## Phase 3  — Dev Stories           [ ] BLOCKED  (requires 2B + 1A + 1B complete)

Also update the BREAKDOWN.md header line:
  Flow        : [PATH CONFIRMED not yet set — written by STEP 13]
→
  Flow        : [Flow Name from PATH CONFIRMED]

Commit both SRS.md and BREAKDOWN.md together:
  git add docs/SRS.md docs/BREAKDOWN.md
  git commit -m "docs(phase-0): PATH CONFIRMED — [Flow Name] locked + BREAKDOWN phases generated"
```

- [ ] **Step 1: Open the original plan and append the amendment section after Task 8's "After 'path confirmed' — write to SRS.md" subsection**

- [ ] **Step 2: Commit the updated plan**

  ```
  git add docs/superpowers/plans/2026-05-17-path-confirmed-implementation.md
  git commit -m "docs(plan): amend Task 8 — add flow-aware BREAKDOWN.md phase generation to STEP 13"
  ```

---

### Task 3: Fix swp-plan.md pre-flight check #3 — PATH CONFIRMED-aware phase gate

**Files:**
- Modify: `.claude/commands/swp-plan.md` (lines ~37–45) — replace hardcoded §1A/§1B/§2A/§2B check with PATH CONFIRMED-aware gate, bump version to 1.8

**Problem:** Pre-flight check #3 always requires §1A + §1B + §2A + §2B to be `[x]`. For Frontend Only (3A), §1A/§1B/§2B never apply. Error messages say "run /swp-ui first" even when the flow uses /swp-design.

- [ ] **Step 1: Replace pre-flight check #3**

  Find this exact block:
  ```
  3. Check docs/BREAKDOWN.md — verify all design phases are signed off before creating dev stories:
       If docs/BREAKDOWN.md does not exist:
         BLOCKED — run /swp-srs first to generate the phase skeleton.
       If docs/BREAKDOWN.md exists, check each line:
         Phase 1A [x] (Architecture)?  — if [ ] pending: BLOCKED — run /swp-arch first
         Phase 1B [x] (Scaffold)?      — if [ ] pending: BLOCKED — run /swp-arch (Stage 2) first
         Phase 2A [x] (UI/UX)?         — if [ ] pending: BLOCKED — run /swp-ui first
         Phase 2B [x] (DB)?            — if [ ] pending: BLOCKED — run /swp-db first
       If all four phases are [x]: output "All design phases approved — proceeding to breakdown." and continue.
  ```

  Replace with:
  ```
  3. Check docs/BREAKDOWN.md — verify all required phases are signed off before creating dev stories:
       If docs/BREAKDOWN.md does not exist:
         BLOCKED — run /swp-srs first to generate the phase skeleton.
       If docs/BREAKDOWN.md exists:
         Read docs/SRS.md PATH CONFIRMED block to determine which phases apply to this flow.
         IF PATH CONFIRMED is missing:
           ⚠️ WARNING — PATH CONFIRMED not found. Checking all phases as fallback.
           Phase 1A [x]? — if [ ] pending: BLOCKED — run /swp-arch first
           Phase 1B [x]? — if [ ] pending: BLOCKED — run /swp-arch (Stage 2) first
           Phase 2A [x]? — if [ ] pending: BLOCKED — run /swp-ui first
           Phase 2B [x]? — if [ ] pending: BLOCKED — run /swp-db first
         IF PATH CONFIRMED found:
           Extract Phase 1 and Phase 2 commands from PATH CONFIRMED.
           For each phase row in BREAKDOWN.md (in order — skip Phase 0 and Phase 3):
             If the row is still [ ] pending:
               BLOCKED — [Phase label] not complete.
               Run [the command shown in that phase row] first.
               Your locked flow: [Flow Name] — [Phase 1 cmd] → [Phase 2 cmd(s)] → /swp-plan
           If all non-Phase-3 rows are [x]: output "All required phases approved for [Flow Name] — proceeding to breakdown." and continue.
  ```

- [ ] **Step 2: Bump version and add changelog entry**

  Change `Version  : 1.7` → `Version  : 1.8` and `Updated  : 2026-05-15` → `Updated  : 2026-05-17`.

  Add changelog row:
  ```
  | 1.8     | 2026-05-17 | Zenthil | Pre-flight #3: PATH CONFIRMED-aware phase gate — reads locked flow, validates only applicable phases |
  ```

- [ ] **Step 3: Commit**

  ```
  git add .claude/commands/swp-plan.md
  git commit -m "fix(swp-plan): pre-flight #3 reads PATH CONFIRMED — validates flow-specific phases only"
  ```

---

### Task 4: Fix swp-status.md — dynamic PATH CONFIRMED-aware dashboard

**Files:**
- Modify: `.claude/commands/swp-status.md` — replace STEP 2 hardcoded dashboard + STEP 3 hardcoded next-action table, bump version to 1.1

**Problem:** Dashboard always shows §1A/§1B/§2A/§2B rows. For any non-full-stack flow, these rows show permanently ⏳ pending giving false status.

- [ ] **Step 1: Replace STEP 2 dashboard block**

  Find this block (lines ~48–76):
  ```
  ## STEP 2 — Build phase dashboard

  Output this dashboard block:

  ```
  ╔══════════════════════════════════════════════════════════════╗
  ║  SmartWorkz Planning Phase Status — [Product Name] [SRS-vX.X] ║
  ╠══════════════════════════════════════════════════════════════╣
  ║  Phase 0  — SRS Review        [STATUS]  [DATE or pending]   ║
  ║  Phase 1A — Architecture      [STATUS]  [DATE or pending]   ║
  ║  Phase 1B — Scaffold          [STATUS]  [DATE or pending]   ║
  ║  Phase 2A — UI/UX Design      [STATUS]  [DATE or pending]   ║
  ║  Phase 2B — DB Design         [STATUS]  [DATE or pending]   ║
  ║  Phase 3  — Dev Stories       [STATUS]  [DATE or BLOCKED]   ║
  ╠══════════════════════════════════════════════════════════════╣
  ║  → Next action: [command to run]                             ║
  ╚══════════════════════════════════════════════════════════════╝
  ```
  ```

  Replace with:
  ```
  ## STEP 2 — Build phase dashboard

  Read docs/SRS.md PATH CONFIRMED block to extract Flow name and phase sequence.

  Build the dashboard rows dynamically from docs/BREAKDOWN.md:
    Read every `## Phase` line in BREAKDOWN.md in order.
    For each line, extract the phase label and status marker ([x] / [ ]).

  Output this dashboard:

  ```
  ╔══════════════════════════════════════════════════════════════════╗
  ║  SmartWorkz Planning Status — [Product Name] [SRS-vX.X]        ║
  ║  Flow: [Flow Name from PATH CONFIRMED — or "Path not locked"]   ║
  ╠══════════════════════════════════════════════════════════════════╣
  ║  [Each ## Phase row from BREAKDOWN.md, in order:]              ║
  ║  [Phase label padded to 30 chars]  [STATUS]  [DATE or pending] ║
  ╠══════════════════════════════════════════════════════════════════╣
  ║  → Next action: [command to run — from STEP 3]                  ║
  ╚══════════════════════════════════════════════════════════════════╝
  ```

  Render each BREAKDOWN.md phase row using these STATUS values:
  ```

- [ ] **Step 2: Replace STEP 3 next-action logic**

  Find lines ~78–94 (STEP 3 hardcoded table). Replace with:
  ```
  ## STEP 3 — Determine next action

  Find the first phase row in BREAKDOWN.md (after Phase 0) that is still `[ ] pending`.

  IF PATH CONFIRMED is found in docs/SRS.md:
    Map the pending phase to its command using the `(command: ...)` annotation in that BREAKDOWN.md row.
    Output: "Run [command] — [phase label]"

  IF PATH CONFIRMED is missing OR BREAKDOWN.md has no phase rows:
    Use this fallback lookup:

    | Condition                                    | Next action output                                            |
    |----------------------------------------------|---------------------------------------------------------------|
    | docs/BREAKDOWN.md does not exist             | `Run /swp-srs to begin — no BREAKDOWN.md found`               |
    | Phase 0 is `[ ]`                             | `Run /swp-srs to complete SRS review`                         |
    | Phase 1A is `[ ]` and Phase 0 is `[x]`      | `Run /swp-arch — architecture design (Stage 1)`               |
    | Phase 1B is `[ ]` and Phase 1A is `[x]`     | `Run /swp-arch — scaffold (Stage 2 auto-continues from 1A)`   |
    | Phase 2A and 2B both `[ ]` (1A+1B done)     | `Run /swp-ui and /swp-db — these can run in parallel`         |
    | Phase 2A is `[ ]` only (2B done)            | `Run /swp-ui to complete UI/UX design`                        |
    | Phase 2B is `[ ]` only (2A done)            | `Run /swp-db to complete DB design`                           |
    | All of 1A+1B+2A+2B are `[x]`                | `Run /swp-plan to create dev stories and ADO tasks`           |
    | Phase 3 is `[ ] in progress`                | `Run /swd-start [Story ID] to continue development`           |
    | All phases `[x]`                             | `All planning phases complete — project in development`        |
  ```

- [ ] **Step 3: Bump version and add changelog entry**

  Change `Version  : 1.0` → `Version  : 1.1`, `Updated  : 2026-05-15` → `Updated  : 2026-05-17`.

  Add:
  ```
  | 1.1     | 2026-05-17 | Zenthil | STEP 2+3: dynamic dashboard reads BREAKDOWN.md rows + PATH CONFIRMED — no more hardcoded phase list |
  ```

- [ ] **Step 4: Commit**

  ```
  git add .claude/commands/swp-status.md
  git commit -m "fix(swp-status): dynamic dashboard reads BREAKDOWN.md + PATH CONFIRMED — flow-aware"
  ```

---

### Task 5: Fix swp-orchestrate.md Phase 0 on-approval — PATH CONFIRMED-aware next phase

**Files:**
- Modify: `.claude/commands/swp-orchestrate.md` (lines ~193–204) — after Phase 0 CP3, read PATH CONFIRMED and show correct next phase, bump version to 1.1

**Problem:** After Phase 0 approval, the orchestrator hardcodes "Proceeding to Phase 1: Architecture Design." For Frontend Only or Mobile Only flows, there is no Phase 1 architecture step.

- [ ] **Step 1: Read swp-orchestrate.md lines 192–205 to confirm exact text**

- [ ] **Step 2: Replace Phase 0 on-approval output block**

  Find:
  ```
  ### Phase 0 — On CP3 approval

  After "SRS approved":
  - Create docs/BREAKDOWN.md with full phase skeleton (if not yet created by /swp-srs)
  - Mark §0 [x] with today's date
  - Commit: `docs(phase-0): SRS approved — BREAKDOWN.md skeleton created`
  - Output:

  ```
  PHASE 0 COMPLETE — SRS approved
  Proceeding to Phase 1: Architecture Design
  ```
  ```

  Replace with:
  ```
  ### Phase 0 — On CP3 approval

  After "SRS approved":
  - BREAKDOWN.md is created by /swp-srs STEP 13 (PATH SELECTION) — do not recreate.
    If BREAKDOWN.md does not exist: warn "BREAKDOWN.md missing — /swp-srs STEP 13 may not have run. Run /swp-srs to complete PATH SELECTION."
  - Read docs/SRS.md PATH CONFIRMED block.
    IF PATH CONFIRMED found: extract Phase 1 command (or "N/A" for frontend-only flows).
    IF PATH CONFIRMED missing: warn "PATH CONFIRMED not locked — run /swp-srs to complete PATH SELECTION."
  - Output:

  ```
  PHASE 0 COMPLETE — SRS approved
  Flow locked: [Flow Name from PATH CONFIRMED]
  Next phase : Run [Phase 1 command from PATH CONFIRMED — or Phase 2 command if Phase 1 is N/A]
  Sequence   : [Phase 1] → [Phase 2] → /swp-plan → dev
  ```
  ```

- [ ] **Step 3: Also update the STEP 0 pipeline dashboard to be flow-aware**

  The dashboard at lines ~55–68 hardcodes six rows. Replace with a dynamic version that reads BREAKDOWN.md rows (same approach as swp-status Task 4):

  Find the six-row dashboard block in STEP 0:
  ```
  ╔════════════════════════════════════════════════════════════════════╗
  ║  SmartWorkz Planning Orchestrator — [Product Name or "New Project"] ║
  ╠════════════════════════════════════════════════════════════════════╣
  ║  Phase 0  — SRS Review           [STATUS]                          ║
  ║  Phase 1A — Architecture Design  [STATUS]                          ║
  ║  Phase 1B — Scaffold             [STATUS]                          ║
  ║  Phase 2A — UI/UX Design         [STATUS]                          ║
  ║  Phase 2B — DB Design            [STATUS]                          ║
  ║  Phase 3  — Dev Stories          [STATUS]                          ║
  ╠════════════════════════════════════════════════════════════════════╣
  ║  Resuming from: [first incomplete phase or "Phase 0 — starting fresh"] ║
  ╚════════════════════════════════════════════════════════════════════╝
  ```

  Replace with:
  ```
  IF docs/BREAKDOWN.md exists AND has flow phase rows:
    Build rows from BREAKDOWN.md (same as /swp-status STEP 2).
    Output:
    ╔════════════════════════════════════════════════════════════════════╗
    ║  SmartWorkz Planning Orchestrator — [Product Name or "New Project"] ║
    ║  Flow: [Flow Name from PATH CONFIRMED — or "Path not yet locked"]  ║
    ╠════════════════════════════════════════════════════════════════════╣
    ║  [Each BREAKDOWN.md phase row, in order, with STATUS]             ║
    ╠════════════════════════════════════════════════════════════════════╣
    ║  Resuming from: [first incomplete phase or "Phase 0 — starting fresh"] ║
    ╚════════════════════════════════════════════════════════════════════╝
  ELSE (BREAKDOWN.md missing or no flow rows yet):
    Output the dashboard with only Phase 0 shown + a note:
    ╔════════════════════════════════════════════════════════════════════╗
    ║  SmartWorkz Planning Orchestrator — [Product Name or "New Project"] ║
    ╠════════════════════════════════════════════════════════════════════╣
    ║  Phase 0  — SRS Review           ⏳ Pending — starting now         ║
    ║  [Remaining phases will be shown after PATH CONFIRMED is locked]   ║
    ╠════════════════════════════════════════════════════════════════════╣
    ║  Resuming from: Phase 0 — starting fresh                          ║
    ╚════════════════════════════════════════════════════════════════════╝
  ```

- [ ] **Step 4: Bump version and add changelog entry**

  Change `Version  : 1.0` → `Version  : 1.1`, `Updated  : 2026-05-15` → `Updated  : 2026-05-17`.

  Add:
  ```
  | 1.1     | 2026-05-17 | Zenthil | Phase 0 on-approval: reads PATH CONFIRMED for next phase; STEP 0 dashboard dynamic from BREAKDOWN.md |
  ```

- [ ] **Step 5: Commit**

  ```
  git add .claude/commands/swp-orchestrate.md
  git commit -m "fix(swp-orchestrate): Phase 0 + STEP 0 dashboard reads PATH CONFIRMED — flow-aware sequence"
  ```

---

### Task 6: Fix swm-refine.md — expand from redirect stub to working command

**Files:**
- Modify: `.claude/commands/swm-refine.md` — replace "delegate to /_skill2.0" stub with actual STEP-based workflow, bump version to 3.0

**Problem:** Running `/swm-refine` outputs "go run /_skill2.0" — not a skill at all.

**Fix:** Add a proper STEP-based invocation that accepts a skill file path, reads it, and runs the full _skill2.0 analysis framework inline.

- [ ] **Step 1: Replace the full file content**

  Replace everything after the frontmatter/changelog with:

  ```markdown
  Refine a ToolKit command skill file: $ARGUMENTS

  Format: /swm-refine <path-to-skill-file>
  Examples:
    /swm-refine .claude/commands/swp-arch.md
    /swm-refine .claude/commands/swd-next.md

  ---

  ## STEP 0 — Input validation

  IF $ARGUMENTS is empty:
    Output:
      USAGE: /swm-refine <path-to-skill-file>
      Example: /swm-refine .claude/commands/swp-arch.md
    STOP.

  Read the file at $ARGUMENTS.
  IF file does not exist: output "File not found: $ARGUMENTS" and STOP.

  ---

  ## STEP 1 — Run _skill2.0 analysis

  Invoke the `_skill2.0` skill with the file content loaded from STEP 0.

  Run the full 6-phase analysis from `_skill2.0`:
    Phase 1: Current Coverage Assessment
    Phase 2: Current vs. Missing Capabilities
    Phase 3: Guideline Compliance Checklist
    Phase 4: Gap Analysis
    Phase 5: Violation Report
    Phase 6: Prioritized Recommendation Roadmap

  Apply all _skill2.0 output formats:
    - Executive Summary
    - Current vs. Missing Capabilities Matrix
    - Coverage Matrix
    - Compliance Scorecard
    - Gap Analysis table
    - Violation Report
    - Recommendation Roadmap (Must Have / Should Have / Nice to Have)

  ---

  ## STEP 2 — Ask for action

  After the full analysis, output:

    REFINEMENT COMPLETE
    ────────────────────────────────────────────────────────────
    File    : $ARGUMENTS
    Score   : [overall compliance %]
    Signal  : [APPROVED / CONDITIONAL / NEEDS WORK]

    Reply with one of:
      "implement must-have"  — apply all Must Have fixes now
      "implement all"        — apply all Must Have + Should Have fixes now
      "fix [N]"              — apply recommendation N only
      "show me [N]"          — explain recommendation N in detail before fixing
    ────────────────────────────────────────────────────────────

  Wait for reply before making any edits.

  ---

  ## STEP 3 — Apply fixes (after reply)

  For each fix requested:
    1. Read the current file content
    2. Apply the exact change described in the recommendation
    3. Verify the fix doesn't break adjacent content
    4. Output: "Fixed [N]: [one-line description of change]"

  After all fixes:
    Bump the skill file's version header by 0.1
    Add a changelog row: `| [version] | [today's date] | Zenthil | Refinement via /swm-refine: [summary of changes] |`
    Commit: `git add $ARGUMENTS && git commit -m "refine([skill-name]): apply /swm-refine recommendations"`
  ```

- [ ] **Step 2: Update version header and changelog in swm-refine.md**

  Change:
  ```
  Version  : 2.0
  Updated  : 2026-05-16
  ```
  to:
  ```
  Version  : 3.0
  Updated  : 2026-05-17
  ```

  Add changelog row:
  ```
  | 3.0     | 2026-05-17 | Zenthil | Expanded from redirect stub to full STEP-based workflow — accepts file path, runs _skill2.0 inline, offers fix actions |
  ```

- [ ] **Step 3: Commit**

  ```
  git add .claude/commands/swm-refine.md
  git commit -m "fix(swm-refine): expand from redirect stub to full STEP-based skill refinement workflow"
  ```
