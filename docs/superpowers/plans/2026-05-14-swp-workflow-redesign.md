# SWP Workflow Redesign — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Restructure 6 skill files so the planning flow runs srs → arch+scaffold → ui+db (parallel) → plan, with BREAKDOWN.md tracking every phase from SRS sign-off onward.

**Architecture:** Each skill file gets surgical edits only — prerequisite blocks, phase labels, BREAKDOWN.md update instructions, and commit messages. No new files created. swp-scaffold.md is marked internal (called by swp-arch Stage 2 only). swp-sync.md is untouched.

**Tech Stack:** Markdown skill files in `.claude/commands/`. All edits are string replacements — no logic, no code generation.

---

## File Map

| File | Change type | Lines affected |
|---|---|---|
| `.claude/commands/swp-srs.md` | Extend BREAKDOWN skeleton, update "Next" prompt | 987–991, 1091, 1094–1097 |
| `.claude/commands/swp-arch.md` | Phase label, prereqs, skill chain, BREAKDOWN §1A, add Stage 2 block | 17, 19–24, 33, 85–88, 916–919, 948, 983, 989–990 |
| `.claude/commands/swp-scaffold.md` | Add internal-only notice at top | line 16 area |
| `.claude/commands/swp-ui.md` | Phase label, add ARCH prereq check, BREAKDOWN §2A mark, commit message labels | 21, 38–41, 2636–2658, 2674, 2678, 2684 |
| `.claude/commands/swp-db.md` | Phase label, add ARCH prereq, BREAKDOWN §2B mark, commit message labels | 6, 29, 237–248, after line 1280, 1293–1359 |
| `.claude/commands/swp-plan.md` | Add BREAKDOWN guard to pre-flight | lines 26–36 |

---

## Task 1: swp-srs.md — Full BREAKDOWN skeleton + updated "Next" prompt

**Files:**
- Modify: `.claude/commands/swp-srs.md:987-991` (BREAKDOWN creation block)
- Modify: `.claude/commands/swp-srs.md:1091` (Next line in output block)
- Modify: `.claude/commands/swp-srs.md:1094-1097` (Prompt tech lead message)

- [ ] **Step 1: Replace the BREAKDOWN.md creation block (Step 12, item 7)**

Find this exact text:
```
7. Update docs/BREAKDOWN.md Phase 0 section:
   Mark "SRS Review" as Done with today's date.
   If docs/BREAKDOWN.md does not exist: create it with Phase 0 section and mark SRS as Done.
     git add docs/BREAKDOWN.md
     (include in the docs commit below)
```

Replace with:
```
7. Create docs/BREAKDOWN.md — full project phase skeleton:
   If docs/BREAKDOWN.md does not exist: create it fresh.
   If it already exists: preserve existing content and add any missing phase sections below.

   Write (or merge) the following structure into docs/BREAKDOWN.md:

   # Project Breakdown
   SRS Version : SRS-v[X.X]
   Generated   : [today's date]
   Generated by: /swp-srs

   ## Phase 0 — SRS Review            [x] [today's date]
     - SRS-v[X.X] approved. [N] Epics, [N] Must Have features.

   ## Phase 1A — Architecture Design   [ ] pending
   ## Phase 1B — Scaffold              [ ] pending
   ## Phase 2A — UI/UX Design          [ ] pending
   ## Phase 2B — DB Design             [ ] pending
   ## Phase 3  — Dev Stories           [ ] BLOCKED (requires 1A+1B+2A+2B complete)

     git add docs/BREAKDOWN.md
     (include in the docs commit below)
```

- [ ] **Step 2: Update the "Next" line in the PHASE 0 COMPLETE output block**

Find:
```
     Next         : Run /swp-ui to design screens, then /swp-db for schema
```

Replace with:
```
     Next         : Run /swp-arch (Phase 1 — architecture must be approved before UI/DB)
```

- [ ] **Step 3: Update the "Prompt tech lead" message**

Find:
```
  "SRS is approved and tagged. Ready to proceed to Phase 0 design gates.
   Run /swp-ui next to design all Must Have screens.
   Or run /swp-db now if you want to start schema design in parallel.
   Run /swp-sync to keep all phase documents current."
```

Replace with:
```
  "SRS is approved and tagged. BREAKDOWN.md created with all phase checkpoints.
   Run /swp-arch next — architecture must be approved before UI/DB design can begin.
   Run /swp-sync to keep all phase documents current."
```

- [ ] **Step 4: Verify the edit**

Open `.claude/commands/swp-srs.md` and confirm:
- BREAKDOWN.md block now contains all 5 phase sections (0 through 3)
- "Next" line says "Run /swp-arch"
- Prompt message says "Run /swp-arch next"

- [ ] **Step 5: Commit**

```bash
git add .claude/commands/swp-srs.md
git commit -m "feat(swp-srs): create full BREAKDOWN.md skeleton at SRS sign-off — all 5 phase checkpoints"
```

---

## Task 2: swp-arch.md — Phase 1, prereqs, skill chain, Stage 2 scaffold block

**Files:**
- Modify: `.claude/commands/swp-arch.md:17` (phase gate label)
- Modify: `.claude/commands/swp-arch.md:19-24` (prerequisite reads)
- Modify: `.claude/commands/swp-arch.md:33` (compatible skill chain)
- Modify: `.claude/commands/swp-arch.md:85-88` (prerequisite check table)
- Modify: `.claude/commands/swp-arch.md:916-919` (BREAKDOWN update in STEP 9)
- Modify: `.claude/commands/swp-arch.md:948` (ADO comment)
- Modify: `.claude/commands/swp-arch.md:983` (output block phase label)
- Modify: `.claude/commands/swp-arch.md:989-990` (output block Next + Stage 2)

- [ ] **Step 1: Update the phase gate label (line 17)**

Find:
```
Phase 2 gate — solution scaffolding is BLOCKED until this design is approved.
```

Replace with:
```
Phase 1 gate — UI/DB design is BLOCKED until architecture and scaffold are approved.
```

- [ ] **Step 2: Update prerequisite reads (lines 19-24)**

Find:
```
**Prerequisite reads before using this command:**
- [SRS.md](../../docs/SRS.md) — Product stack and feature scope (STACK CONFIRMED block)
- [UI-DESIGN.md](../../docs/UI-DESIGN.md) — Frontend specifications (if applicable)
- [DB-DESIGN.md](../../docs/DB-DESIGN.md) — Database schema and stored procedures
- [ENTITIES.md](../../docs/ENTITIES.md) — Entity definitions and mappings
```

Replace with:
```
**Prerequisite reads before using this command:**
- [SRS.md](../../docs/SRS.md) — Product stack and feature scope (STACK CONFIRMED block)
- [ENTITIES.md](../../docs/ENTITIES.md) — Entity definitions pre-seeded from SRS (if it exists)

**Note:** UI-DESIGN.md and DB-DESIGN.md are NOT prerequisites for this command.
Architecture is designed first. UI and DB design use ARCH-DESIGN.md as their prerequisite.
```

- [ ] **Step 3: Update the compatible skill chain (line 33)**

Find:
```
  /swp-srs → /swp-db → /swp-arch → /swp-scaffold → /swd-start
```

Replace with:
```
  /swp-srs → /swp-arch (Stage 1: design → Stage 2: scaffold) → /swp-ui + /swp-db → /swp-plan → /swd-start
```

- [ ] **Step 4: Update the prerequisite check table (lines 85-88)**

Find:
```
  │ docs/UI-DESIGN.md exists (skip if SRS stack has no frontend)            │ ✅/⚠️  │
  │ docs/DB-DESIGN.md exists and is marked approved (if backend uses DB)    │ ✅/⚠️  │
  │ ENTITIES.md exists (if relational DB — skip if NoSQL/document DB)       │ ✅/⚠️  │
  │ docs/BREAKDOWN.md exists and Phase 1 is marked Done                     │ ✅/🔴  │
```

Replace with:
```
  │ ENTITIES.md exists (if relational DB — skip if NoSQL/document DB)       │ ✅/⚠️  │
  │ docs/BREAKDOWN.md exists and Phase 0 (SRS) is marked [x]                │ ✅/🔴  │
```

- [ ] **Step 5: Update the BREAKDOWN.md step in STEP 9 (lines 916-919)**

Find:
```
5. Update docs/BREAKDOWN.md — mark Phase 2 Architecture as Done:
     git add docs/BREAKDOWN.md
     git commit -m "docs(phase-2): BREAKDOWN.md — architecture phase marked Done"
     git push origin develop
```

Replace with:
```
5. Update docs/BREAKDOWN.md — mark §1A Architecture as complete:
   Find: `## Phase 1A — Architecture Design   [ ] pending`
   Replace with:
   `## Phase 1A — Architecture Design   [x] [today's date]`
   `  - Layered architecture: [list layers from approved design]`
   `  - DI container: [from STACK CONFIRMED]`
   `  - Key patterns: [BaseAuditableEntity / ITenantScoped / etc.]`

     git add docs/BREAKDOWN.md
     git commit -m "docs(phase-1): BREAKDOWN.md — §1A architecture approved"
     git push origin develop

   Then auto-continue to Stage 2 (do not wait for user input):
   Output: "Stage 1 approved — auto-continuing to Stage 2: Scaffold"
```

- [ ] **Step 6: Update the ADO comment (line 948)**

Find:
```
     Comment : "Phase 2 Architecture approved. ARCH-DESIGN.md committed. /swp-scaffold is unblocked."
```

Replace with:
```
     Comment : "Phase 1A Architecture approved. ARCH-DESIGN.md committed. Auto-continuing to Stage 2: Scaffold."
```

- [ ] **Step 7: Update the output block phase label and add Stage 2 (lines 982-990)**

Find:
```
9. Output:
     PHASE 2 ARCHITECTURE COMPLETE
     Projects         : [N] ([N] test projects incl. IntegrationTests)
     Base classes     : [N] scaffolded
     Mapping strategy : [choice]
     State strategy   : [choice]
     Result<T>        : [adopted / not adopted]
     Root files       : global.json, .editorconfig, Directory.Build.props created
```

Replace with:
```
9. Output:
     STAGE 1 ARCHITECTURE APPROVED
     Projects         : [N] ([N] test projects incl. IntegrationTests)
     Base classes     : [N] scaffolded
     Mapping strategy : [choice]
     State strategy   : [choice]
     Result<T>        : [adopted / not adopted]
     Root files       : global.json, .editorconfig, Directory.Build.props created
     Next             : Auto-continuing to Stage 2 — Scaffold

---

## STAGE 2 — Scaffold

Runs automatically after Stage 1 is approved. Do NOT wait for user input to begin.

Output: "▶ Stage 2: Scaffold — running /swp-scaffold steps against approved ARCH-DESIGN.md"

Execute all steps from /swp-scaffold (commands/.claude/commands/swp-scaffold.md):
- Read docs/ARCH-DESIGN.md (just approved in Stage 1)
- Run STEP 0 through STEP 11 of swp-scaffold
- Build verification: zero errors, zero warnings required

### Stage 2 approval gate

  —————————————————————————————————————————————————
  STAGE 2 GATE — Scaffold
  Build output : [zero errors / zero warnings — confirmed]
  Projects     : [N] created and building
  Base files   : [N] scaffolded

  To approve scaffold : "scaffold approved"

  Claude will not mark Phase 1 complete until "scaffold approved".
  —————————————————————————————————————————————————

[STOP — wait for "scaffold approved"]

### After "scaffold approved"

1. Update docs/BREAKDOWN.md — mark §1B Scaffold as complete:
   Find: `## Phase 1B — Scaffold              [ ] pending`
   Replace with:
   `## Phase 1B — Scaffold              [x] [today's date]`
   `  - Build: 0 errors, 0 warnings`
   `  - Projects: [N] created`

     git add docs/BREAKDOWN.md
     git commit -m "docs(phase-1): BREAKDOWN.md — §1B scaffold approved"
     git push origin develop

2. Update ADO work item:
     State   : Done
     Comment : "Phase 1B Scaffold approved. Zero build errors. /swp-ui and /swp-db are now unblocked."

3. Output:
     PHASE 1 COMPLETE — Architecture + Scaffold
     §1A Architecture : [x] approved
     §1B Scaffold     : [x] approved
     Next             : Run /swp-ui and /swp-db — these can run in parallel (both read ARCH-DESIGN.md)
```

- [ ] **Step 8: Verify the edit**

Open `.claude/commands/swp-arch.md` and confirm:
- Phase gate says "Phase 1 gate"
- Prerequisite reads no longer list UI-DESIGN.md or DB-DESIGN.md
- Skill chain shows the new order
- Prerequisite check table has no UI/DB design rows
- STEP 9 item 5 updates §1A
- Stage 2 section exists at end of file with its own sign-off gate
- "After scaffold approved" block updates §1B

- [ ] **Step 9: Commit**

```bash
git add .claude/commands/swp-arch.md
git commit -m "feat(swp-arch): redesign as two-stage command — Phase 1 gate, Stage 2 scaffold, BREAKDOWN §1A+§1B"
```

---

## Task 3: swp-scaffold.md — Mark as internal-only

**Files:**
- Modify: `.claude/commands/swp-scaffold.md:16` (phase gate line area)

- [ ] **Step 1: Add internal-only notice above the phase gate line**

Find:
```
Phase 2 gate — development is BLOCKED until the solution builds with zero errors and zero warnings.
```

Replace with:
```
> ⚠️ INTERNAL COMMAND — called by /swp-arch Stage 2 only. Do not run this command standalone.
> If you need to scaffold the solution, run /swp-arch — it will auto-continue to scaffold after Stage 1 (architecture) is approved.

Phase 2 gate — development is BLOCKED until the solution builds with zero errors and zero warnings.
```

- [ ] **Step 2: Verify**

Open `.claude/commands/swp-scaffold.md` and confirm the internal notice appears before the phase gate line.

- [ ] **Step 3: Commit**

```bash
git add .claude/commands/swp-scaffold.md
git commit -m "feat(swp-scaffold): mark as internal — called by swp-arch Stage 2 only"
```

---

## Task 4: swp-ui.md — Phase 2 gate, ARCH prereq, BREAKDOWN §2A

**Files:**
- Modify: `.claude/commands/swp-ui.md:21` (phase gate label)
- Modify: `.claude/commands/swp-ui.md:37-41` (pre-flight checks block)
- Modify: `.claude/commands/swp-ui.md:2636-2646` (Step 7.3 BREAKDOWN update)
- Modify: `.claude/commands/swp-ui.md:2650-2658` (README/CHANGELOG phase labels)
- Modify: `.claude/commands/swp-ui.md:2674,2678,2684` (phase labels in output)

- [ ] **Step 1: Update the phase gate label (line 21)**

Find:
```
Phase 1 gate — DB design and development are BLOCKED until this review is complete and approved.
```

Replace with:
```
Phase 2 gate — DB design and development are BLOCKED until this review is complete and approved.
```

- [ ] **Step 2: Add ARCH-DESIGN.md pre-flight checks (lines 37-41)**

Find:
```
  PRE-FLIGHT:
  SRS found              : ✅ / ❌ STOP — run /swp-srs first
  STACK CONFIRMED found  : ✅ / ❌ STOP — run /swp-arch first
  CONTEXT.md found       : ✅ / ❌ STOP — run /swp-plan first
  UI-DESIGN.md exists    : YES (incremental update — read existing screens) / NO (fresh design)
```

Replace with:
```
  PRE-FLIGHT:
  SRS found                         : ✅ / ❌ STOP — run /swp-srs first
  STACK CONFIRMED found             : ✅ / ❌ STOP — run /swp-arch first
  ARCH-DESIGN.md found              : ✅ / ❌ STOP — run /swp-arch first (Phase 1 must be complete)
  BREAKDOWN.md §1A [x] confirmed    : ✅ / ❌ STOP — architecture not approved, run /swp-arch
  CONTEXT.md found                  : ✅ / ❌ STOP — run /swp-plan first
  UI-DESIGN.md exists               : YES (incremental update — read existing screens) / NO (fresh design)
```

- [ ] **Step 3: Update Step 7.3 BREAKDOWN section (lines 2636-2646)**

Find:
```
### 7.3 — Update docs/BREAKDOWN.md

For each screen and service, add a task row in the relevant Epic section:
  | [ScreenName]Page component          | UI — Component   | [Story ID] | Todo |
  | [FeatureName]StateService / Store   | UI — State       | [Story ID] | Todo |
  | [SharedComponentName]               | UI — Shared      | [Story ID] | Todo |

### 7.4 — Commit UI design files

  git add docs/UI-DESIGN.md docs/BREAKDOWN.md CONTEXT.md
  git commit -m "docs(phase-1): UI/UX design approved — [N] screens, [N] components, [N] routes"
```

Replace with:
```
### 7.3 — Update docs/BREAKDOWN.md

Mark §2A complete and append design summary:

  Find: `## Phase 2A — UI/UX Design          [ ] pending`
  Replace with:
  `## Phase 2A — UI/UX Design          [x] [today's date]`
  `  - Screens: [N] — [N] PAGE, [N] MODAL, [N] WIZARD, [N] WIDGET`
  `  - Dark mode: confirmed`
  `  - WCAG: AA compliant`

For each screen and service, add a task row in the relevant Epic section:
  | [ScreenName]Page component          | UI — Component   | [Story ID] | Todo |
  | [FeatureName]StateService / Store   | UI — State       | [Story ID] | Todo |
  | [SharedComponentName]               | UI — Shared      | [Story ID] | Todo |

Check if §2B (DB Design) is also [x] in BREAKDOWN.md. If both §2A and §2B are [x]:
  Find: `## Phase 3  — Dev Stories           [ ] BLOCKED (requires 1A+1B+2A+2B complete)`
  Replace with: `## Phase 3  — Dev Stories           [ ] READY — run /swp-plan to create dev stories`

### 7.4 — Commit UI design files

  git add docs/UI-DESIGN.md docs/BREAKDOWN.md CONTEXT.md
  git commit -m "docs(phase-2): UI/UX design approved — [N] screens, [N] components, [N] routes"
```

- [ ] **Step 4: Update README/CHANGELOG phase labels (lines 2650-2658)**

Find:
```
  Phase badge  → "Phase 1 — UI Design Approved"
```
Replace with:
```
  Phase badge  → "Phase 2 — UI Design Approved"
```

Find:
```
  - Phase 1 UI: [N] screens across [N] Epics — [N] shared components, [N] feature-local, [N] routes defined
```
Replace with:
```
  - Phase 2 UI: [N] screens across [N] Epics — [N] shared components, [N] feature-local, [N] routes defined
```

Find:
```
  git commit -m "docs(phase-1): update README and CHANGELOG — UI design approved"
```
Replace with:
```
  git commit -m "docs(phase-2): update README and CHANGELOG — UI design approved"
```

- [ ] **Step 5: Update completion output phase labels (lines 2674, 2678)**

Find:
```
   If RESULT: ✅ CONSISTENT → output "PHASE 1 UI COMPLETE — /swp-sync validated."
```
Replace with:
```
   If RESULT: ✅ CONSISTENT → output "PHASE 2 UI COMPLETE — /swp-sync validated."
```

Find:
```
  PHASE 1 UI COMPLETE
```
Replace with:
```
  PHASE 2 UI COMPLETE
```

- [ ] **Step 6: Verify**

Open `.claude/commands/swp-ui.md` and confirm:
- Phase gate says "Phase 2 gate"
- Pre-flight has ARCH-DESIGN.md and §1A checks
- Step 7.3 marks §2A [x] and checks §2B for Phase 3 unlock
- Commit messages say "phase-2"
- Completion output says "PHASE 2 UI COMPLETE"

- [ ] **Step 7: Commit**

```bash
git add .claude/commands/swp-ui.md
git commit -m "feat(swp-ui): Phase 2 gate, add ARCH-DESIGN prereq, BREAKDOWN §2A mark on sign-off"
```

---

## Task 5: swp-db.md — Phase 2 gate, ARCH prereq, BREAKDOWN §2B

**Files:**
- Modify: `.claude/commands/swp-db.md:6` (description phase label)
- Modify: `.claude/commands/swp-db.md:29` (phase gate line)
- Modify: `.claude/commands/swp-db.md:237-248` (STEP 0 pre-flight)
- Modify: `.claude/commands/swp-db.md` after line 1280 (add §2B BREAKDOWN block)
- Modify: `.claude/commands/swp-db.md:1293,1297,1302,1309-1313,1353,1359` (phase labels)

- [ ] **Step 1: Update the description phase label (line 6)**

Find:
```
  This is a Phase 1 gate — development is blocked until the database design is reviewed and approved.
```

Replace with:
```
  This is a Phase 2 gate — development is blocked until the database design is reviewed and approved.
```

- [ ] **Step 2: Update the phase gate line (line 29)**

Find:
```
Phase 1 gate — development is BLOCKED until this review is complete and approved.
```

Replace with:
```
Phase 2 gate — development is BLOCKED until this review is complete and approved.
```

- [ ] **Step 3: Add ARCH-DESIGN.md prereq to STEP 0 pre-flight (lines 237-248)**

Find:
```
  PRE-FLIGHT:
  [ ] docs/SRS.md exists and has STACK CONFIRMED block
  [ ] Database engine identified (SQL Server / PostgreSQL / etc.)
  [ ] ORM identified (EF Core / Dapper / raw SQL)
  [ ] All Epics and their entities are listed in the SRS
  [ ] Multi-tenancy strategy is confirmed (row-level TenantId / schema-per-tenant / db-per-tenant)
```

Replace with:
```
  PRE-FLIGHT:
  [ ] docs/SRS.md exists and has STACK CONFIRMED block
  [ ] docs/ARCH-DESIGN.md exists — if not: STOP — run /swp-arch first (Phase 1 must be complete)
  [ ] docs/BREAKDOWN.md §1A [x] confirmed — if not: STOP — architecture not approved, run /swp-arch
  [ ] docs/ENTITIES.md exists (pre-seeded by /swp-srs) — if not: STOP — run /swp-srs first
  [ ] Database engine identified (SQL Server / PostgreSQL / etc.)
  [ ] ORM identified (EF Core / Dapper / raw SQL)
  [ ] All Epics and their entities are listed in the SRS
  [ ] Multi-tenancy strategy is confirmed (row-level TenantId / schema-per-tenant / db-per-tenant)
```

- [ ] **Step 4: Add §2B BREAKDOWN update block after line 1280 (after "Update ENTITIES.md")**

Find:
```
2. Update ENTITIES.md with all new table rows (append under correct schema heading, use enriched format from Step 7).

3. Update SRS.md — patch the Database section:
```

Replace with:
```
2. Update ENTITIES.md with all new table rows (append under correct schema heading, use enriched format from Step 7).

2b. Update docs/BREAKDOWN.md — mark §2B DB Design as complete:
   Find: `## Phase 2B — DB Design             [ ] pending`
   Replace with:
   `## Phase 2B — DB Design             [x] [today's date]`
   `  - Tables: [N], SPs: [N]`
   `  - Migration plan: included`
   `  - PII tables: [N] classified`

   Check if §2A (UI Design) is also [x] in BREAKDOWN.md. If both §2A and §2B are [x]:
     Find: `## Phase 3  — Dev Stories           [ ] BLOCKED (requires 1A+1B+2A+2B complete)`
     Replace with: `## Phase 3  — Dev Stories           [ ] READY — run /swp-plan to create dev stories`

3. Update SRS.md — patch the Database section:
```

- [ ] **Step 5: Update all phase labels in commit messages and output (lines 1293–1359)**

Find and replace each of the following:

```
git commit -m "docs(phase-1): DB architecture approved — [N] tables, [N] SPs across [N] schemas"
```
→
```
git commit -m "docs(phase-2): DB architecture approved — [N] tables, [N] SPs across [N] schemas"
```

```
git commit -m "feat(phase-1): SP stubs scaffolded — [N] SPs across [N] schemas"
```
→
```
git commit -m "feat(phase-2): SP stubs scaffolded — [N] SPs across [N] schemas"
```

```
  Phase badge  → "Phase 1 — DB Architecture Approved"
```
→
```
  Phase badge  → "Phase 2 — DB Architecture Approved"
```

```
     - Phase 1 DB: [N] schemas, [N] tables, [N] stored procedures designed
     - PII inventory: [N] tables classified, security review CLEAR / WARNINGS noted
```
→
```
     - Phase 2 DB: [N] schemas, [N] tables, [N] stored procedures designed
     - PII inventory: [N] tables classified, security review CLEAR / WARNINGS noted
```

```
  git commit -m "docs(phase-1): update README and CHANGELOG — DB architecture approved"
```
→
```
  git commit -m "docs(phase-2): update README and CHANGELOG — DB architecture approved"
```

```
     PHASE 1 DB COMPLETE
```
→
```
     PHASE 2 DB COMPLETE
```

```
     Next          : Run /swp-arch then /swp-scaffold to create the solution structure
```
→
```
     Next          : Run /swp-plan (once §2A UI Design is also complete — check BREAKDOWN.md)
```

- [ ] **Step 6: Verify**

Open `.claude/commands/swp-db.md` and confirm:
- Phase gate says "Phase 2 gate" in both description and body
- Pre-flight has ARCH-DESIGN.md and §1A checks + ENTITIES.md check
- New §2B BREAKDOWN block exists after "Update ENTITIES.md"
- All "phase-1" in commit messages changed to "phase-2"
- "PHASE 2 DB COMPLETE" in output
- "Next: Run /swp-plan" (not /swp-arch)

- [ ] **Step 7: Commit**

```bash
git add .claude/commands/swp-db.md
git commit -m "feat(swp-db): Phase 2 gate, add ARCH-DESIGN prereq, BREAKDOWN §2B mark on sign-off"
```

---

## Task 6: swp-plan.md — BREAKDOWN guard in pre-flight

**Files:**
- Modify: `.claude/commands/swp-plan.md:26-36` (pre-flight block)

- [ ] **Step 1: Add BREAKDOWN guard as pre-flight check 3**

Find:
```
BEFORE STARTING — pre-flight checks, then read files:

PRE-FLIGHT:
1. Verify docs/SRS.md exists → if missing: STOP immediately — "docs/SRS.md not found. Cannot begin breakdown."
2. Check ADO MCP availability → if unavailable: note it and continue through Levels 1–4 normally; when Level 4.5 is reached, output "ADO MCP unavailable — stopping here. Re-run /swp-plan when MCP is restored to create ADO items in Level 5." and STOP at that point.

READ (after pre-flight passes):
1. docs/SRS.md — full document, especially STACK CONFIRMED block and all feature sections
2. ENTITIES.md — registered tables (do not create entities already listed)
3. docs/BREAKDOWN.md — if it exists, load current status and existing approved items
```

Replace with:
```
BEFORE STARTING — pre-flight checks, then read files:

PRE-FLIGHT:
1. Verify docs/SRS.md exists → if missing: STOP immediately — "docs/SRS.md not found. Cannot begin breakdown."
2. Check ADO MCP availability → if unavailable: note it and continue through Levels 1–4 normally; when Level 4.5 is reached, output "ADO MCP unavailable — stopping here. Re-run /swp-plan when MCP is restored to create ADO items in Level 5." and STOP at that point.
3. Check docs/BREAKDOWN.md — verify all design phases are signed off before creating dev stories:
     If docs/BREAKDOWN.md does not exist:
       BLOCKED — run /swp-srs first to generate the phase skeleton.
     If docs/BREAKDOWN.md exists, check each line:
       Phase 1A [x] (Architecture)?  — if [ ] pending: BLOCKED — run /swp-arch first
       Phase 1B [x] (Scaffold)?      — if [ ] pending: BLOCKED — run /swp-arch (Stage 2) first
       Phase 2A [x] (UI/UX)?         — if [ ] pending: BLOCKED — run /swp-ui first
       Phase 2B [x] (DB)?            — if [ ] pending: BLOCKED — run /swp-db first
     If all four phases are [x]: output "All design phases approved — proceeding to breakdown." and continue.

READ (after pre-flight passes):
1. docs/SRS.md — full document, especially STACK CONFIRMED block and all feature sections
2. ENTITIES.md — registered tables (do not create entities already listed)
3. docs/BREAKDOWN.md — load current status and all approved phase items (required — confirmed in pre-flight check 3)
```

- [ ] **Step 2: Verify**

Open `.claude/commands/swp-plan.md` and confirm:
- Pre-flight now has 3 checks (not 2)
- Check 3 reads BREAKDOWN.md and blocks on any phase not [x]
- BLOCKED messages name the correct command to run for each missing phase
- READ item 3 no longer says "if it exists" — it's now required

- [ ] **Step 3: Commit**

```bash
git add .claude/commands/swp-plan.md
git commit -m "feat(swp-plan): add BREAKDOWN guard — blocks until all design phases §1A+1B+2A+2B are approved"
```

---

## Final verification

- [ ] **Step 1: Confirm complete flow**

Read all 6 modified files in sequence and verify the end-to-end chain:

```
/swp-srs sign-off
  → creates BREAKDOWN.md with §0[x], §1A[ ], §1B[ ], §2A[ ], §2B[ ], §3[BLOCKED]
  → Next says "Run /swp-arch"

/swp-arch Stage 1 sign-off
  → §1A = [x]
  → auto-continues to Stage 2

/swp-arch Stage 2 sign-off
  → §1B = [x]
  → Next says "Run /swp-ui and /swp-db (parallel)"

/swp-ui sign-off
  → §2A = [x]
  → checks if §2B also [x] → if yes, §3 = READY

/swp-db sign-off
  → §2B = [x]
  → checks if §2A also [x] → if yes, §3 = READY

/swp-plan pre-flight
  → reads BREAKDOWN.md
  → blocks if any of §1A, §1B, §2A, §2B is [ ]
  → proceeds only when all four are [x]
```

- [ ] **Step 2: Final commit with version bumps**

For each modified skill file, update the version header and revision history row:

| File | Version bump | Change summary |
|---|---|---|
| swp-srs.md | 4.3 → 4.4 | BREAKDOWN.md full skeleton at sign-off |
| swp-arch.md | 3.1 → 3.2 | Phase 1 gate, Stage 2 scaffold, BREAKDOWN §1A+1B |
| swp-scaffold.md | 1.3 → 1.4 | Internal-only notice |
| swp-ui.md | 1.8 → 1.9 | Phase 2 gate, ARCH prereq, BREAKDOWN §2A |
| swp-db.md | 2.4 → 2.5 | Phase 2 gate, ARCH prereq, BREAKDOWN §2B |
| swp-plan.md | 1.6 → 1.7 | BREAKDOWN guard in pre-flight |

```bash
git add .claude/commands/swp-srs.md .claude/commands/swp-arch.md .claude/commands/swp-scaffold.md .claude/commands/swp-ui.md .claude/commands/swp-db.md .claude/commands/swp-plan.md
git commit -m "chore(swp-workflow): bump versions — swp-srs 4.4, swp-arch 3.2, swp-scaffold 1.4, swp-ui 1.9, swp-db 2.5, swp-plan 1.7"
```
